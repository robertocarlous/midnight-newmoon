# Persisted so every `npm run build` picks up the deployed contract without
# needing to remember to pass VITE_CONTRACT_ADDRESS via shell env each time
# (a build without it silently falls back to the DeployPanel instead of the
# board - bit us once already).
VITE_CONTRACT_ADDRESS=079616525b2f3fbcedfcf7f21795a8453508825c8e51958a4e013ae48e06da3c
